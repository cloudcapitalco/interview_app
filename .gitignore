# Internal agent/design tooling
.claude/
.agents/

# Challenge data — provided separately (via pre-signed links), not committed to this repo
data/candidate_dataset.parquet
data/pricing_options_filtered.parquet
data/*.parquet
data/*.json
data/*.csv

# Common environment cruft (stack is candidate's choice; cover the usual suspects)
.venv/
__pycache__/
*.pyc
node_modules/
dist/
build/
.DS_Store

# Local reference to the other interview — not part of the candidate repo
data_interview
