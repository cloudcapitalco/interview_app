[project]
name = "cloudcapital-economics-service"
version = "0.1.0"
description = "Cloud Capital — commitment economics service (provided to candidates; modifiable)"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.0",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "pydantic>=2",
]

[tool.uv]
package = false
